# ember-scripts-windows

Usage
-----
Add directory containing these scripts to PATH and then run `ember-serve.bat` from an unprivileged bash prompt to launch an ember server with elevated privileges.
